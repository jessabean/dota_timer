// Generated by CoffeeScript 1.6.3
var PORT, PROTOCOL, ROOT, app, express, fs, http, io, latestTime, path, redis, server, sio;

require("coffee-script");

io = require('socket.io');

fs = require('fs');

express = require('express');

path = require('path');

app = express();

redis = require('redis');

PROTOCOL = "http";

PORT = 8090;

ROOT = path.dirname(__dirname);

if (PROTOCOL === "http") {
  http = require("http");
  server = http.createServer(app);
}

server.listen(PORT);

app.use('/public', express["static"]("" + ROOT + "/public"));

app.use('/css', express["static"]("" + ROOT + "/css"));

app.use('/js', express["static"]("" + ROOT + "/js"));

app.use('/img', express["static"]("" + ROOT + "/img"));

app.get("/*", function(req, res) {
  return res.sendfile("" + ROOT + "/index.html");
});

sio = io.listen(server);

sio.enable("browser client minification");

sio.enable("browser client gzip");

sio.set("log level", 1);

latestTime = new Date();

sio.sockets.on('connection', function(socket) {
  socket.emit('clock_reset', {
    newTime: latestTime.toJSON()
  });
  return socket.on('timer_was_reset', function() {
    console.log;
    latestTime = new Date();
    socket.emit('clock_reset', {
      newTime: latestTime.toJSON()
    });
    return socket.broadcast.emit('clock_reset', {
      newTime: latestTime.toJSON()
    });
  });
});
